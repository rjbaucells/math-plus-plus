#include <gtest/gtest.h>
#include "math++/math.h"

// TEST(Matrix, power_iteration) {
//     // arrange
//     constexpr Matrix<2, 2> m = {{6, 4}, {7, 1}};

//     // act
//     auto [eigenVec, eigenVal] = m.powerIteration(10000);

//     // assert
//     ASSERT_TRUE(eigenVec * eigenVal == m * eigenVec);
// }
